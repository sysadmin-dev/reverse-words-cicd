apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: reverse-words
  annotations:
    kubernetes.io/ingress.class: nginx
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  rules:
  - host: reversewords-prod.apps.okd.ag04.io
    http:
      paths:
      - backend:
          serviceName: reverse-words
          servicePort: http
